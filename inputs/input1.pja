class gcd_rec
{
	public static void main(String[] args)
	{
		int a, b;
		a = 315;
		b = 1932;
		System.out.println(gcd(a,b));
	}

	int gcd(int a, int b)
	{
		if (a == b || b == 0)
			return a;

		if (a == 0)
			return b;

		if (a > b)
			return gcd(a % b, b);

		return gcd(a, b % a);
	}
}

